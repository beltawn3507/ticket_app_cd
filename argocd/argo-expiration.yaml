apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: expiration-app
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/beltawn3507/ticket_app_cd.git
    targetRevision: main
    path: expiration   # <-- directory containing deployment.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
